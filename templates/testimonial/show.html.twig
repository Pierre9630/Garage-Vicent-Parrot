{% extends 'base.html.twig' %}

{% block title %}Avis{% endblock %}

{% block body %}
    <h1 class="text-center m-3">Avis</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Nom</th>
                <td>{{ testimonial.name }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ testimonial.lastname }}</td>
            </tr>
            {#  <tr>
                <th>Phone</th>
                <td>{{ testimonial.phone }}</td>
            </tr>#}
            <tr>
                <th>Message</th>
                <td>{{ testimonial.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_testimonial_index') }}"><i class="fa-solid fa-arrow-left"></i><span class="sr-only">Retour</span></a>

    <a href="{{ path('app_testimonial_edit', {'id': testimonial.id}) }}"><i class="fa-solid fa-pen-to-square"></i><span class="sr-only">Modifier</span></a>

    {{ include('testimonial/_delete_form.html.twig') }}
{% endblock %}
