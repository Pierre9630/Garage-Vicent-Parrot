{% extends 'base.html.twig' %}

{% block title %}Avis{% endblock %}

{% block body %}
    <h1>Avis</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Name</th>
                <td>{{ testimonial.name }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ testimonial.lastname }}</td>
            </tr>
            {#  <tr>
                <th>Phone</th>
                <td>{{ testimonial.phone }}</td>
            </tr>#}
            <tr>
                <th>Message</th>
                <td>{{ testimonial.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_testimonial_index') }}"><i class="fa-solid fa-arrow-left"></i></a>

    <a href="{{ path('app_testimonial_edit', {'id': testimonial.id}) }}"><i class="fa-solid fa-pen-to-square"></i></a>

    {{ include('testimonial/_delete_form.html.twig') }}
{% endblock %}
