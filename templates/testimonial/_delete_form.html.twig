<form method="post" action="{{ path('app_testimonial_delete', {'id': testimonial.id}) }}" onsubmit="return confirm('Voulez vous supprimer cet avis?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ testimonial.id) }}">
    <button class="btn"><i class="fa-solid fa-trash"></i><span class="sr-only">Supprimer</span></button>
</form>
