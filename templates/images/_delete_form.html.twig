<form method="post" action="{{ path('app_images_delete', {'id': image.id}) }}" onsubmit="return confirm('Voulez vous supprimer cette image?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image.id) }}">
    <button class="btn">Delete</button>
</form>
