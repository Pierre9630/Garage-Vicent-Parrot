/*document.addEventListener('DOMContentLoaded', function() {
    const ratingElement = document.getElementById('testimonial_note');
    const ratingValue = parseFloat(ratingElement.dataset.rating) || 0;

    new RateYo({
        element: ratingElement,
        rating: ratingValue,
        readOnly: true,
        starWidth: '20px'
    });
});*/
